apiVersion: v1
kind: Service
metadata:
  name: weconnect-redis
  labels:
    app: weconnect-redis
spec:
  selector:
    app: weconnect-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
