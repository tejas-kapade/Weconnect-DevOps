apiVersion: apps/v1
kind: Deployment
metadata:
  name: weconnect-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weconnect-redis
  template:
    metadata:
      labels:
        app: weconnect-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
