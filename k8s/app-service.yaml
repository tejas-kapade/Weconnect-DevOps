apiVersion: v1
kind: Service
metadata:
  name: weconnect-service
  labels:
    app: weconnect-app
spec:
  type: LoadBalancer
  selector:
    app: weconnect-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
